version: "3.9"
services:
    project:
        build: .
        image: one030914/ai_project # add this line
        container_name: project
        stdin_open: true
        tty: true
        command: bash
        gpus: all
        working_dir: /app
        volumes:
            - .:/app